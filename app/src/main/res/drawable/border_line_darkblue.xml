<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Background color (if needed) -->
    <item android:drawable="@color/white" />

    <!-- Border line -->
    <item
        android:top="-2dp"
        android:left="-2dp"
        android:right="-2dp"
        android:drawable="@drawable/border_line_two" />
</layer-list>
